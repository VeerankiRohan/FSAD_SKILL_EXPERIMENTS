# Server Port
server.port=9093

# Application Name
spring.application.name=Skill_experiment-9

# Show detailed error (optional for debugging)
server.error.include-message=always
server.error.include-binding-errors=always

# Logging (optional)
logging.level.org.springframework=INFO